{
	"name": "sellmystuff-server",
	"version": "0.1.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"lint": "eslint \"**/*.{js,jsx}\"",
		"test": "ENV=test jest --watch",
		"format": "prettier --write \"**/*.{js, jsx, css, json}\"",
		"format:check": "prettier --list-different \"**/*.{js, jsx, css, json}\"",
		"dev": "ENV=test nodemon"
	},
	"jest": {
		"testEnvironment": "node"
	},
	"author": "Marco Kunz",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.16.4",
		"pg": "^7.6.1"
	},
	"devDependencies": {
		"jest": "^23.6.0",
		"prettier": "^1.15.2",
		"supertest": "^3.3.0"
	}
}